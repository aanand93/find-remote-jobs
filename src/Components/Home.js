import React from 'react';

const Home = () => {
	return (
		<div>
			<h1>Find Remote Jobs</h1>
			<h5>Welcome to my remote job search app. Click the jobs tab to begin!</h5>
		</div>
	);
};

export default Home;
